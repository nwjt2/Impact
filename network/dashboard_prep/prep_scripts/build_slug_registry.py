"""Build master_slug_registry.csv from the catalogue CSVs.

Per advice doc lesson 2: NEVER hand-edit. Always regenerate.

Phase 1 stub — full implementation lands in Phase 6 (dashboard prep).
"""
from __future__ import annotations


def main() -> None:
    raise NotImplementedError(
        "Phase 1 stub. Full implementation in Phase 6 once we have scraper "
        "output to register. Will read all four catalogue CSVs + relationship "
        "CSVs and emit master_slug_registry.csv with one canonical slug per "
        "entity, plus aliases."
    )


if __name__ == "__main__":
    main()
