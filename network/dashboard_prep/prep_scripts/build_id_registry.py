"""Build master_id_registry.csv. UIDs are stable across runs (lesson 5).

Phase 1 stub.
"""
from __future__ import annotations


def main() -> None:
    raise NotImplementedError(
        "Phase 1 stub. Full implementation in Phase 6. Will assign and persist "
        "UIDs across runs so identifiers don't churn (lesson 5: two-run "
        "retention)."
    )


if __name__ == "__main__":
    main()
